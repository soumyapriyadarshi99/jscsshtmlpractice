// import { addition } from "./addnumber";
const res = require("express/lib/response");

describe("", () => {
  if (
    ("addition check",
    () => {
      let result = addition(10, 20);
      expect(result).toBe(30);
    })
  );
});
